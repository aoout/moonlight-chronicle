/* =========================================================
   蚀月远征 · System 基类
   所有 ECS System 继承此基类
   ========================================================= */

export class System {
  /** 系统唯一标识符 */
  name = 'unnamed';

  /** 注册时调用，用于初始化 */
  init() {}

  /** 每帧逻辑更新（固定时间步长内调用） */
  update(dt) {}

  /** 每帧渲染（可选，由渲染循环调用） */
  render(ctx) {}

  /** 游戏状态切换时调用（如进入/离开 PLAYING 状态） */
  onStateChange(from, to) {}
}